export default {
    // Put rotating proxy here: http://username:password@ip:port
    // Write %SESSION% if you want random rotating sessions
    proxy: '',
    timeout: 5000,
    threads: 100,
    maxChecks: 100000
}